# Multi-resource mutation audit

## Method and scope

This inventory was completed before changing tests or production code. The search followed every production caller of `MutationCoordinator` and `MutationPlanner`, then every production `fs::rename`, `fs::remove_file`, `atomic_create`/`atomic_replace`, attachment no-replace install, and CAS `store`/`increment_ref`/`decrement_ref` call. API handlers and offline CLI/watcher entry points are listed below. SQLite indexes and the feed database are included when a filesystem mutation treats them as a second resource; read-only routes and derived-index-only rebuilds are excluded.

Classifications mean:

- **Batch coordinator**: every file in the logical unit is represented by one durable `BatchMutationCommand`; failure rolls back all published intents, and a post-filesystem index failure retains recoverable transaction state.
- **Single-file coordinator**: one authored file is the logical commit; publication and index reconciliation are serialized and compensated by `MutationCoordinator`.
- **Single-resource atomic publication**: one file or directory entry is the complete authored resource. No cross-file atomicity is claimed.
- **Compensated external-resource transaction**: resources cannot share one filesystem transaction. The implementation commits one resource and explicitly compensates it if the other resource fails; compensation errors remain actionable.
- **Per-item commits**: a loop is deliberately not all-or-none. Each item is independently committed and represented by one result entry.
- **Derived-resource reconciliation**: the authored file is authoritative and the second store is rebuilt/reconciled rather than presented as an atomic peer.

## Inventory

| Operation (entry point / symbol evidence) | Logical unit | Resources | Current compensation | Classification | Failure proof test |
|---|---|---|---|---|---|
| Create a page (`src/api/pages.rs::create_default_page`, `create_page`; `src/api/index_routes.rs::create_from_link`; `src/api/base_members.rs::create_base_member_with_ids`; `src/api/journal.rs::ensure_journal`; `src/api/conversations.rs::capture_new`) | One page creation | Markdown page + SQLite index | `MutationCoordinator::create_page` unlinks a published page if index creation fails, rolls back partial index state, and suppresses notification | Single-file coordinator | `tests/api_test.rs::page_create_rolls_back_file_and_exact_uuid_when_index_publication_fails`; `page_create_rolls_back_exact_path_and_uuid_after_publication_failure` |
| Update a page (`src/api/pages.rs::update_page_at_path`, protect/unprotect transition; `src/api/academic.rs::mutate_academic_page`; `src/api/journal.rs::capture_today`; `src/api/conversations.rs::capture_existing`; `src/api/board/tasks.rs::patch_task`) | One existing page revision | Markdown page + SQLite index | Exact expected bytes are checked under the path lock; a stale writer is rejected and index failure is surfaced without a second file commit | Single-file coordinator | `tests/api_test.rs::page_update_rejects_stale_revision_without_changing_file`; `concurrent_academic_updates_return_one_stale_conflict` |
| Span-preserving page edits (`src/api/blocks.rs::assign_block_id`; `src/api/properties.rs::patch_properties`; `src/api/tasks.rs::update_task_status`) | One page content revision | Markdown page + SQLite index | `MutationCoordinator::replace_page_content` uses exact expected bytes and reconciles the index after publication | Single-file coordinator | `tests/api_test.rs::page_update_rejects_stale_revision_without_changing_file` plus coordinator cancellation coverage in `src/vault/mutation_coordinator.rs::tests::cancelling_post_publish_wait_keeps_path_locked_until_index_phase_finishes` |
| Move page and rewrite inbound links (`src/api/pages.rs::move_page`, `MutationPlanner::plan(MovePage)`) | Source page move plus every backlink rewrite | Multiple Markdown pages/directories + SQLite index + move hooks | Durable batch stages exact planner snapshots; publication failure rolls every path back and emits no notification | Batch coordinator | `src/vault/mutation_coordinator.rs::tests::plan_derived_move_rolls_back_backlink_before_primary_failure_without_notification`; `tests/api_test.rs::page_move_commits_primary_and_backlink_rewrite_before_one_notification` |
| Delete page and rewrite inbound links (`src/api/pages.rs::delete_page`, `MutationPlanner::plan(DeletePage)`) | Deleted page plus every backlink rewrite | Multiple Markdown pages + SQLite index + delete hooks | Durable batch rollback restores deleted bytes and rewritten pages; hooks run after filesystem commit during index reconciliation | Batch coordinator | `tests/mutation_test.rs::batch_publication_failure_rolls_back_without_notification`; `tests/archive_test.rs::archive_delete_decrements_cas_ref_count` |
| Assign one page (`src/api/pages.rs::assign_page`) | One page metadata/path projection | Markdown page (and possible move) + SQLite index | In-place assignments use single-file coordinator update; path-changing assignment is one planner-derived durable batch | Single-file coordinator / batch coordinator when moved | `tests/api_test.rs::page_mutation_project_assignment_set_clear_and_unchanged_preserve_contracts`; batch rollback proof above |
| Bulk assign (`src/api/pages.rs::assign_bulk`) | Every requested page | Multiple Markdown pages/moves + SQLite index | One `BatchMutationCommand`; missing/stale page or injected publication failure rejects the whole request, preserving all files | Batch coordinator | `tests/api_test.rs::bulk_assign_rolls_back`; `bulk_assign_reports_canonical_paths_for_normalizing_aliases` |
| Move folder and rewrite links (`src/api/folders.rs::move_folder`, `MutationPlanner::plan(MoveFolder)`) | Exact planner inventory under the source folder plus backlink rewrites | Multiple files/directories + SQLite index + move hooks | One durable batch; source inventory is snapshotted once, destination directories are transaction-owned, unexpected late content prevents destructive cleanup | Batch coordinator | `tests/mutation_test.rs::folder_plan_batch_uses_the_planner_inventory_without_rewalking`; `empty_folder_plan_does_not_adopt_a_late_file_during_conversion`; batch recovery directory tests in `src/vault/batch_mutation.rs` |
| Delete folder recursively (`src/api/folders.rs::delete_folder`, `MutationCoordinator::delete_folder`) | Folder subtree | All files in subtree + SQLite index + delete hooks | Coordinator holds subtree exclusion, removes files, applies delete hooks, and reconciles index; this path is not backlink-rewriting planner execution | Coordinated subtree mutation | `tests/archive_test.rs::delete_folder_recursive_runs_delete_hooks`; `src/mcp/server.rs::tests::folder_delete_of_nonempty_folder_needs_recursive` |
| Create empty folder (`src/api/folders.rs::create_folder`) | One directory entry | Vault directory | Create-only directory operation; no index resource and no multi-file guarantee | Single-resource directory creation; no multi-resource “safe” claim | Proof gap recorded: `tests/api_test.rs::create_and_list_folder` proves publication, but no deterministic I/O-failure injection exists |
| Close cycle with task carryover (`src/api/board/cycles.rs::patch_cycle`, `plan_cycle_patch_and_carryover`) | Cycle page plus every carried task | Multiple Markdown pages + SQLite index | All final bytes are prepared from exact snapshots and published in one batch | Batch coordinator | `tests/api_board_test.rs::closing_cycle_rolls_back_cycle_and_every_carried_task_on_failure` |
| Create/update board cycle or task without carryover (`src/api/board/cycles.rs::create_cycle`; `src/api/board/tasks.rs::{create_task,patch_task}`) | One cycle or task page | Markdown page + SQLite index | Uses coordinator create/update; no loop-level partial state | Single-file coordinator | `tests/api_test.rs::page_create_rolls_back_file_and_exact_uuid_when_index_publication_fails`; stale-update proof above |
| Reserved feed manifest membership changes (`src/api/feeds.rs::{subscribe_feed,update_feed,delete_feed,import_opml}` via `publish_manifest`) | One `feeds.md` revision | `feeds.md` + derived feed SQLite state | `write_reserved_manifest` performs exact-byte conditional publication; feed store is reconciled from the authoritative manifest and warning state preserves the last good subscription set | Derived-resource reconciliation | `src/api/feeds.rs::tests::publication_race_to_invalid_utf8_returns_the_raw_revision_conflict`; `warning_manifest_preserves_the_last_good_subscription_set`; `stale_import_revision_precedes_every_opml_parse_rejection` |
| Capture conversation (`src/api/conversations.rs::{capture_new,capture_existing}`) | One conversation page per call | Markdown page + SQLite index + in-memory identity ledger | Coordinator create/update commits the page before ledger response; retry identity selects the same page | Single-file coordinator | `src/mcp/server.rs::tests::capture_conversation_error_does_not_expose_raw_source_turn_id`; generic create/index rollback proof above |
| Upload/delete attachment (`src/api/attachments.rs::{upload_attachment,delete_attachment}`; `install_noreplace`; `tokio::fs::remove_file`) | One attachment | One attachment directory entry | Upload streams to a synced temporary file and atomically installs no-replace; temporary-file RAII cleans failed uploads. Delete removes exactly one validated regular file | Single-resource atomic publication | `tests/api_test.rs::upload_attachment_conflict`; attachment traversal/not-found tests in the same file |
| Create/update/delete base definition (`src/api/bases.rs` through `src/vault/base_document.rs::{create,update,delete}`) | One `.base` definition | One `.base` file | Create/replace uses atomic publication and exact revision checks; delete checks the revision immediately before removal | Single-resource atomic publication | `src/vault/base_document.rs::tests::stale_update_does_not_touch_file`; `stale_delete_does_not_touch_file`; `publication_errors_preserve_the_atomic_failure_phase` |
| Set up/rewrap encryption keyring (`src/api/encryption.rs::{setup_encryption,rewrap_wrapped_identity}`; `src/vault/keyring.rs`) | Keyring metadata and optional wrapped-identity file | `.clepsydra/crypto/keyring.toml` + owner-only identity file | Setup removes a newly created identity if metadata create fails. Rewrap removes a newly created identity when metadata replacement was not published; published-but-not-durable state is reported rather than claimed rolled back | Compensated multi-file publication | `tests/api_encryption_test.rs::keyring_setup_read_duplicate_and_revision_locked_rewrap_contract` (duplicate/stale failures preserve the readable pair) |
| Archive ingestion (`src/api/archive.rs::ingest_archive`; `ContentStore::store`; `MutationCoordinator::create_page`) | Archive page plus one reference for each unique uploaded CAS blob | Markdown page + SQLite index + CAS SQLite ref-counts/blob files | Every successful CAS store/increment is recorded. Any later page failure decrements every recorded reference; a coordinator failure after page publication also removes/reconciles the page. CAS remains a separate compensated resource, not part of the filesystem batch | Compensated external-resource transaction | `tests/archive_test.rs::rollback_on_index_failure_preserves_primary_and_compensates_every_blob`; Task 6 adds `tests/api_test.rs::archive_*` proofs at the post-CAS and post-page-publication boundaries |
| Delete archive page/folder (`src/vault/archive_hook.rs::ArchiveDeleteHook::on_page_deleted`) | Deleted archive page and its unique blob references | Markdown page batch + CAS reference rows | Delete hook decrements each unique referenced hash after filesystem commit. CAS garbage collection later removes zero-reference blobs | Compensated external-resource transaction | `tests/archive_test.rs::archive_delete_decrements_cas_ref_count`; `delete_folder_recursive_runs_delete_hooks` |
| BibTeX import loop (`src/api/academic.rs::import_bibtex`) | One independently reported bibliography entry | One work page + SQLite index per item | Each `create_work_internal` is a coordinator commit; errors become an `ImportResult { status: "error" }` and later entries continue | Per-item commits | Existing item-level response behavior is exercised by import tests in `tests/import_test.rs`; the same boundary is pinned for Zotero by the Task 6 failure proof |
| DOI/ISBN import (`src/api/academic.rs::{import_doi,import_isbn_handler}`) | One requested work | One work page + SQLite index | One coordinator create; endpoint returns one result or one typed error | Single-file coordinator | Generic create/index rollback proof in `tests/api_test.rs::page_create_rolls_back_file_and_exact_uuid_when_index_publication_fails` |
| Zotero import loop (`src/api/academic.rs::import_zotero_handler`) | One independently reported Zotero item per commit | One work page + SQLite index per item; optional checkpoint file after run | Create, provenance, and existing-item update failures are converted to an `ImportResult { status: "error" }` for that item and later items continue. Checkpoint is best-effort and counts only `created` results; no all-or-none batch contract is declared | Per-item commits | `tests/api_test.rs::import_zotero_*`; `tests/academic_test.rs::zotero_item_two_update_failure_is_reported_without_rolling_back_item_one`; `zotero_item_two_provenance_failure_is_reported_after_item_one_commits` |
| Watcher reconciliation move (`src/vault/reconcile.rs::reconcile_page`, called by `src/lib.rs::reconcile_upserts`) | Canonical move plus backlink rewrites | Multiple Markdown pages + SQLite index + hooks | Planner conversion executes through `MutationCoordinator::execute_batch_direct`; watcher holds the same path guard as API writes | Batch coordinator | `tests/mutation_test.rs::move_plan_batch_intents_cover_every_previewed_path_with_exact_expected_bytes`; `src/lib.rs` watcher race tests |
| Offline relabel (`src/bin/cli.rs::Commands::Relabel`; `src/vault/relabel.rs::relabel`) | One canonical page move plus its backlink rewrites per iteration | Multiple Markdown pages + SQLite index | Each planned page relabel is one direct coordinator batch; completed relabels are independent commits, and a later failure does not claim all-run atomicity | Per-page batch commits | `tests/mutation_test.rs::batch_publication_failure_rolls_back_without_notification`; `src/bin/cli.rs::tests::relabel_dry_run_returns_zero` proves dry-run non-mutation |
| Offline legacy frontmatter migration (`src/bin/cli.rs::Commands::Migrate`; `src/vault/migrate.rs::migrate`) | One legacy page per conversion | One Markdown page | Each page uses `atomic_replace`; failures are reported as warnings and other pages continue. The command is explicitly per-page, not all-or-none | Per-page single-resource atomic publication | `tests/frontmatter_migration.rs::dry_run_reports_and_writes_nothing`; `write_converts_all_legacy_pages_and_is_idempotent`; deterministic publication-failure proof is a recorded gap |
| Offline new note (`src/bin/cli.rs::Commands::New`) | One note | One Markdown page | Vault create-only publication; no index is open in this offline command | Single-resource publication; no multi-resource “safe” claim | `src/bin/cli.rs::tests::new_creates_a_note`; deterministic I/O-failure proof is a recorded gap |
| Offline backup (`src/bin/cli.rs::Commands::Backup`; `src/vault/backup.rs::create_backup`) | One backup archive | Temporary partial archive + final archive entry | Partial archive removes itself on drop; successful commit atomically preserves an existing same-name destination | Single-resource atomic publication | `src/vault/backup.rs::tests::partial_archive_removes_its_file_when_dropped_without_commit`; `preserves_an_existing_archive_for_the_same_timestamp` |
| Offline init/config create (`src/bin/cli.rs::{Commands::Init,ConfigCommands::Create}`; `src/config_command.rs::create`) | New vault scaffold or one application config | Newly created directories/files | Create-only semantics; config removes its just-created file after an incomplete write. Neither operation promises rollback of an entire pre-existing tree | Create-only compensated scaffold | `src/config_command.rs::tests::create_refuses_to_overwrite_existing_config`; `src/bin/cli.rs::tests::init_creates_a_vault`; deterministic incomplete-write injection is a recorded gap |

## Conclusions before Task 6 code changes

1. Page move/delete, folder move, watcher reconciliation, cycle carryover, and bulk assignment are correctly classified as durable batch-coordinator operations. They must not gain secondary executors or per-path success responses.
2. Archive ingestion necessarily spans the vault filesystem/index and the independent CAS SQLite/blob store. Its valid model is explicit, idempotent compensation with retained primary and cleanup errors; it must not be described as cross-resource filesystem atomicity.
3. Zotero and BibTeX endpoints expose ordered per-item `results`, so their valid boundary is one independently reported item per commit. No endpoint contract declares an all-or-none import.
4. Task 6 therefore adds deterministic post-CAS and post-page-publication archive proofs and an item-two Zotero failure proof. Production changes are permitted only if those tests expose a mismatch between the classifications above and observable state/response behavior.
