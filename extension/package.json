{
	"name": "clepsydra-web-archive",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite build --watch",
		"build": "vite build && bun run verify:bundle",
		"build:firefox": "TARGET=firefox vite build && bun run verify:bundle dist-firefox",
		"build:safari": "TARGET=safari vite build && bun run verify:bundle dist-safari",
		"safari:xcode": "bun run build:safari && xcrun safari-web-extension-packager dist-safari --project-location safari --app-name 'Clepsydra Web Archive' --bundle-identifier app.clepsydra.Clepsydra-Web-Archive --macos-only --swift --no-open --no-prompt",
		"test": "vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit",
		"lint": "biome check src/",
		"verify:bundle": "node scripts/verify-bundle.mjs"
	},
	"dependencies": {
		"@mozilla/readability": "^0.5.0",
		"single-file-core": "1.5.84",
		"turndown": "^7.2.0",
		"turndown-plugin-gfm": "^1.0.2"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@types/turndown": "^5.0.6",
		"chrome-types": "^0.1.0",
		"typescript": "^5.6.0",
		"vite": "^6.0.0",
		"vite-plugin-web-extension": "^4.0.0",
		"vitest": "^3.0.0"
	}
}
