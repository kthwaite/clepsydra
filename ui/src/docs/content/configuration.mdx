export const meta = {
  slug: "configuration",
  title: "Configuration",
  description: "Configure server and vault behavior plus Status, theme, and chrome preferences."
}

Clepsydra uses two config layers:

1. **Application config** (`config.toml`) — server bind settings, frontend serving mode, TLS, vault root, and feed scheduling/limits
2. **Vault config** (`<vault>/.clepsydra/config.toml`) — vault behavior, academic/archive settings

## Status and preferences

Choose **STATUS** in the desktop header (the mobile control is labelled
**Status**) or run **Open Status / preferences** in the command console. Both
open the Settings dialog directly on **Appearance**. The folio sidebar’s
**Settings** control opens the same dialog.

- **General** shows current corpus and index counts.
- **Navigation** selects Smart, New Tab, or Replace behavior for opening pages.
- **Appearance** controls mode, accent, density, and diegetic chrome.
- **Location** saves coordinates and an optional label to
  `.clepsydra/location.toml` for Atrium’s sunrise, sunset, and day arc.
- **Editor** currently displays informational placeholders; it has no shipped
  preference controls.
- **Advanced** owns encrypted-note setup/password changes and index-health
  actions. These are operational controls, not visual preferences.

### Theme toggle

Use the header button labelled **[DARK]** or **[PAPER]**, run **Toggle dark
mode**, or choose Dark/Paper under **Status → Appearance**. The direct toggle
switches between the resolved dark and paper palettes. Appearance also offers
the shipped accent palettes and compact/default/spacious density.

Theme, accent, and density are browser-local preferences stored in
`localStorage` (`clepsydra.theme`, `clepsydra.accent`, and
`clepsydra.density`). They persist for that browser origin; they do not modify
`config.toml`, vault files, or other clients.

### Diegetic chrome

Use **Status → Appearance → Diegetic chrome** or run **Toggle diegetic
chrome**. Turning it off suppresses the ornamental footer annotations such as
the VESSEL/write indicator and index-collation text; primary navigation, page
content, query, Status, and the theme toggle remain available. The setting is
on by default and persists for the browser origin under
`clepsydra.diegetic`; it does not change vault data.

The privacy boundary for these preferences is distinct from protected folios:
browser-local appearance values are not synchronized or encrypted by
Clepsydra, while `.clepsydra/location.toml` is a plaintext vault-internal file.

---

## 1) Application config (`config.toml`)

### Lookup order

Application config is required by `serve`, `new`, `backup`, `relabel`,
`migrate`, `grep`, `tree`, `mcp`, and `open-url`. The LSP uses it only when no
workspace `.clepsydra` root resolves; `doctor` can diagnose its absence. The
`config show` and `config path` commands use this same lookup. Clepsydra checks:

1. `./config.toml` (current working directory)
2. `$XDG_CONFIG_HOME/clepsydra/config.toml`
3. `$HOME/.config/clepsydra/config.toml`

### Inspecting and creating the file

```bash
clep config show
clep config show --origin
clep config path
clep config path --trace
clep config create
```

`config show` prints the first existing file from the lookup order byte for
byte on stdout, with no added newline and no stderr output. `--origin` leaves
stdout unchanged and writes `Origin: <selected path>\n` to stderr.

`config path` writes only `<selected path>\n` to stdout and nothing to stderr.
With `--trace`, stderr lists only the candidates actually considered, in
lookup order: unselected candidates have a two-space prefix and the selected
candidate has a `→ ` prefix. Lookup stops at the selected file, so the trace
does not include later candidates.

Origin and trace diagnostics use automatic color only on stderr when supported.
Redirected stderr is plain text, `NO_COLOR=1` disables ANSI color, and stdout
is never styled.

`config create` creates a fully commented user config template at
`$XDG_CONFIG_HOME/clepsydra/config.toml`, or
`$HOME/.config/clepsydra/config.toml` when `XDG_CONFIG_HOME` is unset. It
creates missing parent directories and refuses to overwrite an existing file.
The non-empty template documents precedence and defaults in commented
`[server]`, `[server.tls]`, `[vault]`, `[features]`, and `[feeds]` sections;
all example assignments remain commented, so the template parses as an empty
TOML table.
The compact active equivalent is shown below.

### Example

```toml
[server]
host = "localhost"
port = 3000
dev_mode = false

[server.tls]
enabled = false
# cert_path = "certs/localhost.pem"
# key_path = "certs/localhost-key.pem"

[vault]
root = "./vault"

[features]
academic = true
feeds = true

[feeds]
fetch_interval_minutes = 30
retention_days = 30
unread_retention_days = 90
max_response_bytes = 10485760
max_entry_content_bytes = 1048576
fetch_concurrency = 4
```

### Keys

| Section | Key | Type | Default | Notes |
|---|---|---:|---|---|
| `server` | `host` | string | `localhost` | Bind host |
| `server` | `port` | integer | `3000` | Bind port |
| `server` | `dev_mode` | bool | `false` | `false`: serve embedded frontend. `true`: API/docs only |
| `server.tls` | `enabled` | bool | `false` | Enable HTTPS |
| `server.tls` | `cert_path` | string | unset | Used only when **both** cert and key are set |
| `server.tls` | `key_path` | string | unset | Used only when **both** cert and key are set |
| `vault` | `root` | string | `./vault` | Vault root path |
| `features` | `academic` | bool | `true` | Enable Academic routes and UI |
| `features` | `feeds` | bool | `true` | Enable Feeds routes, UI, storage, networking, and scheduler |
| `feeds` | `fetch_interval_minutes` | integer | `30` | Base refresh interval; failures use bounded backoff |
| `feeds` | `retention_days` | integer | `30` | Retain read, unbookmarked entries |
| `feeds` | `unread_retention_days` | integer | `90` | Retain unread, unbookmarked entries |
| `feeds` | `max_response_bytes` | integer | `10485760` | Maximum bytes streamed from one response |
| `feeds` | `max_entry_content_bytes` | integer | `1048576` | Larger entry HTML is omitted rather than truncated |
| `feeds` | `fetch_concurrency` | integer | `4` | Maximum simultaneous feed fetches |

`features.academic` and `features.feeds` are startup feature flags. Both
default to `true`. Changes take effect only after restarting Clepsydra.
Setting a flag to `false` disables that feature but does not delete its files;
re-enabling it uses the preserved data.

### Frontend serving behavior (`server.dev_mode`)

- `dev_mode = false` (default): Clepsydra serves embedded frontend assets from `ui/dist` at `/`.
- `dev_mode = true`: frontend serving is disabled; use the Vite dev server separately (typically `http://127.0.0.1:5173`).

### API bind and trust boundary (`server.host`)

Clepsydra does not configure API users, sessions, bearer tokens, or API keys.
Any process that can reach the bound HTTP(S) server can call the API. The
default `localhost` host keeps the listener on loopback. Binding a LAN, tailnet,
or wildcard address expands the trust boundary; combine it with a separately
controlled network policy and TLS valid for the hostname clients use.

Use `http://<client-host>:<port>` when TLS is disabled and
`https://<client-host>:<port>` when enabled. `client-host` must be a reachable
name or interface address, not necessarily the raw bind value. In particular,
`0.0.0.0` and `::` mean “bind all” and are not client destinations; use an
appropriate interface address or `localhost` from the same machine. Bracket an
IPv6 literal (`https://[::1]:3000`). Swagger UI and the generated contract are
served at `/api/docs` and `/api/openapi.json`; see the
[API Reference](/docs/api-reference).

### TLS behavior (`server.tls`)

When `server.tls.enabled = true`:

1. If both `cert_path` and `key_path` are set, Clepsydra uses those certificate files.
2. Otherwise, Clepsydra attempts automatic localhost certificate provisioning with `mkcert`.

Auto-generated certs are stored in your platform app-data dir under `clepsydra/`, for example:

- macOS cert: `~/Library/Application Support/clepsydra/localhost.pem`
- macOS key: `~/Library/Application Support/clepsydra/localhost-key.pem`

If `mkcert` is unavailable, startup fails with an installation hint.

To start an HTTPS server without changing this file — the usual case when
testing an HTTPS-only client against a machine that already runs a plain server
— use the `serve` flags instead:

```bash
clep serve --tls --port 3443
```

See [CLI](/docs/cli#clep-serve) for the complete flags and failure behavior.

### RSS/Atom behavior (`feeds`)

Subscriptions are stored in the exact root file `<vault>/feeds.md`. Second-level
headings define groups, list items define feed URLs, optional Markdown link text
defines a title override, and trailing hashtags define feed-level tags:

```markdown
## Engineering
- [Rust Blog](https://blog.rust-lang.org/feed.xml) #rust #work
- https://example.com/atom.xml
```

`feeds.md` is a reserved manifest, not an indexed page. Clepsydra excludes that
exact root path even when `vault.excluded_patterns` is replaced; a nested
`notes/feeds.md` remains an ordinary page. API operations live under
`/api/vault/feeds`, and membership changes require the current manifest revision
so external edits cannot be overwritten.

Every request and redirect is checked against non-global destinations, response
bytes and stored entry HTML are bounded by the configured limits, and persisted
HTML is sanitized. Bookmarked entries are never pruned. Read and unread
unbookmarked entries use `retention_days` and `unread_retention_days`
respectively.

### Environment overrides

Settings-backed commands support env overrides via `CLEPSYDRA__...`:

- `CLEPSYDRA__SERVER__HOST`
- `CLEPSYDRA__SERVER__PORT`
- `CLEPSYDRA__SERVER__DEV_MODE`
- `CLEPSYDRA__SERVER__TLS__ENABLED`
- `CLEPSYDRA__SERVER__TLS__CERT_PATH`
- `CLEPSYDRA__SERVER__TLS__KEY_PATH`
- `CLEPSYDRA__VAULT__ROOT`
- `CLEPSYDRA__FEATURES__ACADEMIC`
- `CLEPSYDRA__FEATURES__FEEDS`
- `CLEPSYDRA__FEEDS__FETCH_INTERVAL_MINUTES` (and the same hierarchy for every `[feeds]` key)

`clep new` is intentionally narrower: it uses the same file lookup but reads
only `[vault].root` from the selected file. For that command the key is required;
`CLEPSYDRA__VAULT__ROOT` and the Settings default do not apply.

Example:

```bash
export CLEPSYDRA__SERVER__DEV_MODE=true
export CLEPSYDRA__FEATURES__ACADEMIC=false
export CLEPSYDRA__FEATURES__FEEDS=false
export CLEPSYDRA__SERVER__PORT=4000
cargo run --bin clep -- serve
```

For Settings-backed commands, full precedence is: defaults → config file →
environment → `serve --tls` / `serve --port`.

### Relative path resolution

- `vault.root` for Settings-backed commands:
  - absolute → used as-is
  - relative in `config.toml` → resolved relative to that config file's directory
  - relative from `CLEPSYDRA__VAULT__ROOT` → resolved relative to process working directory
  - leading `~` → expanded to the home directory
- `clep new` resolves a relative file value against the config file's directory,
  but does not expand `~` or accept the environment override.
- `server.tls.cert_path` / `server.tls.key_path`: the Settings-backed rules above
  apply (file-relative or process-relative by source, with `~` expansion).

---

## 2) Vault config (`.clepsydra/config.toml`)

Created by:

```bash
clep init <path>
# or
cargo run --bin clep -- init <path>
```

Path:

```text
<vault-root>/.clepsydra/config.toml
```

### `[vault]` settings

| Key | Type | Default | Notes |
|---|---:|---|---|
| `attachment_folder` | string | `_attachments` | Attachment root under vault |
| `excluded_patterns` | string[] | see below | Glob/prefix excludes |
| `default_page_folder` | string | `""` | Used by `clep new` |
| `linkable_properties` | string[] | `["tags", "aliases"]` | Frontmatter fields treated as refs |
| `disambiguation_strategy` | enum | `shortest_path` | `shortest_path`, `closest_directory`, `most_recent` |

Default `excluded_patterns`:

```toml
[
  ".clepsydra",
  ".clepsydra/**",
  "_attachments",
  "_attachments/**",
  ".git",
  ".git/**",
  "node_modules",
  "node_modules/**",
]
```

The exact root `feeds.md` is always excluded independently of this list.

### `[academic]` settings

| Key | Default |
|---|---|
| `library_folder` | `library` |
| `papers_folder` | `library/papers` |
| `books_folder` | `library/books` |
| `annotations_folder` | `library/annotations` |

### `[archive]` settings

| Key | Type | Default |
|---|---:|---|
| `enabled` | bool | `true` |
| `cas_path` | string | `~/.clepsydra/cas` |
| `default_path_prefix` | string | `archive` |
| `max_blob_size_mb` | integer | `100` |
| `max_request_size_mb` | integer | `250` |
| `gc_min_age_days` | integer | `30` |

Notes:

- `cas_path` supports `~/...` (expanded to your home directory).
- Archive request size limits are enforced at ingest time. `max_blob_size_mb`
  bounds one deconstructed resource; `max_request_size_mb` bounds the whole
  capture (the deconstructed snapshot plus every resource it references).
  Both budget decoded bytes.
- The HTTP body limit on `POST /api/vault/archive` is derived from
  `max_request_size_mb`, not equal to it: the wire payload is JSON carrying
  base64, which inflates decoded bytes by 4/3, so the body limit is
  `max_request_size_mb * 4 / 3` MB. This keeps the 400 from the resource-size
  check firing before a bare 413 would.

### Encrypted-note files and lock behavior

Encrypted-note key material is stored under the vault's excluded internal
directory:

```text
<vault-root>/.clepsydra/crypto/
├── keyring.toml
└── <key-id>.identity.age   # present for password-based setup
```

- `keyring.toml` contains the keyring version, active key ID, public age
  recipient, and optional wrapped-identity filename.
- `<key-id>.identity.age` contains the password-wrapped age identity. It is
  sensitive ciphertext: include it in protected backups, do not publish it,
  and keep a separate recovery identity.
- On Unix, Clepsydra requests mode `0700` for the directory and `0600` for its
  files. Filesystem or platform policy may provide additional controls.

There is no encrypted-note password or plaintext identity in `config.toml`.
There is also no user-facing idle-lock key in v1: inactivity locking is disabled
by default. The production frontend clears its in-memory identity on reload or
close and provides coordinated manual locking. Custom frontend hosts can supply
an in-memory idle timeout to `EncryptionProvider`; it is not a server setting.

### Protected notes and plaintext attachments

Protected-note encryption covers the Markdown body, including an attachment
reference saved in that body. It does not cover attachment bytes, filenames,
vault-relative paths, reported MIME types, or sizes. All remain plaintext and
can disclose an attachment's subject or content. Open
[attachment management](#attachment-management) from an editable note to work
with these files and references.

#### Attachment management

For a protected note, Clepsydra discloses plaintext storage separately for each
upload and each insertion of an already stored attachment reference. Cancelling
an upload disclosure stores no attachment and inserts no reference. An
acknowledged upload sends the file with
`plaintext_acknowledged=true`; the server rejects an absent or false field and
does not install the file. The field acknowledges plaintext storage—it does not
encrypt, authorize, or prevent an upload.

Custom clients must also send `plaintext_acknowledged=true` for every upload
and must present equivalent plaintext disclosure. It can nevertheless send the
field without user interaction or insert a reference without confirmation:
reference confirmation is enforced by the Clepsydra frontend, not the server.
Only the Markdown reference is encrypted with a protected body; the attachment
remains plaintext.

The stale-reference audit is a client-only, best-effort check. It recognizes
only Markdown links and images that target Clepsydra attachment URLs. It
ignores raw HTML, code, malformed or unresolved links, and non-attachment
destinations.

A stale-reference warning is shown only while attachment management is open
for an unlocked protected note and the current attachment inventory has loaded
successfully. If the inventory cannot load, no stale-reference warning is
shown. Absence of a warning is not proof that no stale reference exists. The
browser sends neither decrypted note content nor its extracted attachment
reference inventory to the server.

See `docs/encrypted-notes.md` for setup, recovery, limitations, and the
complete security boundary.

---

## Full vault config example

```toml
[vault]
attachment_folder = "_attachments"
default_page_folder = "notes"
linkable_properties = ["tags", "aliases", "related"]
disambiguation_strategy = "closest_directory"
excluded_patterns = [
  ".clepsydra/**",
  "_attachments/**",
  ".git/**",
  "node_modules/**",
  "build/**",
]

[academic]
library_folder = "library"
papers_folder = "library/papers"
books_folder = "library/books"
annotations_folder = "library/annotations"

[archive]
enabled = true
cas_path = "~/.clepsydra/cas"
default_path_prefix = "archive"
max_blob_size_mb = 100
max_request_size_mb = 250
gc_min_age_days = 30
```

---

## Related docs

- `/docs/getting-started`
- `/docs/cli`
- `docs/encrypted-notes.md`
- `ui/README.md`
- `extension/README.md`
